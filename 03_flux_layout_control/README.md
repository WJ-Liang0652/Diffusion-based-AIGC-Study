# FLUX Layout Control

This directory contains FLUX-specific experiments. The first experiment is a fixed-configuration `FLUX.1-schnell` baseline, kept separate from the existing SDXL reference work.
